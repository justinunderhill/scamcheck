# Runtime dependencies for the Vercel Python serverless function (api/index.py).
# Mirrors backend/pyproject.toml minus dev/test tooling and the local server.
# Note: the `anthropic` SDK is intentionally omitted — the deployment runs
# without an ANTHROPIC_API_KEY, so the AI layer short-circuits before importing
# it (deterministic template summaries). Add it here when wiring AI on Vercel.
fastapi>=0.110
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
tldextract>=5.1
python-whois>=0.9
confusable-homoglyphs>=3.3
