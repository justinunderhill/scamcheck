# Runtime dependencies for the Vercel Python serverless function (api/index.py).
# Mirrors backend/pyproject.toml minus dev/test tooling and the local server.
fastapi>=0.110
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
tldextract>=5.1
python-whois>=0.9
confusable-homoglyphs>=3.3
# AI layer. Only actually invoked when ANTHROPIC_API_KEY is set AND a durable
# rate-limit store (Upstash Redis) is configured — see the interlock in
# routes/check.py and services/limits.py.
anthropic>=0.49
